{
  "scripts": {
    "build": "npx tailwindcss-cli@latest build ./tailwind.css -o ./style/main.css",
    "prod": "NODE_ENV=production npx tailwindcss-cli@latest build ./tailwind.css -o ./style/main.css"
  },
  "dependencies": {
    "tailwindcss": "^2.0.2"
  },
  "devDependencies": {
    "git-scripts": "^0.4.3",
    "surge": "^0.21.7"
  },
  "git": {
    "scripts": {
      "pre-push": "yarn prod && surge --project ./ --domain apricots-concept.surge.sh"
    }
  },
  "name": "apricots-concept",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT"
}
